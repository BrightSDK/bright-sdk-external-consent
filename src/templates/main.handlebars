<div class="header">
  <img src="{{logo}}" alt="App Logo">
  <div class="title">{{title}}</div>
</div>
<div class="body">
  {{> consent-body}}
</div>
<div class="buttons">
  {{> buttons}}
</div>
<div class="footer">
  {{> footer}}
</div>
